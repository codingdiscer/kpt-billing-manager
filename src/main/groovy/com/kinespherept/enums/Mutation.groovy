package com.kinespherept.enums

/**
 * Flag used by Griffon model classes to signify if an action represents a "create new" or "edit existing" paradigm.
 */
enum Mutation {
    CREATE, UPDATE, UNSET
}
